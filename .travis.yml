language: generic
install:
 - curl -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=stable -y
 - export PATH=$HOME/.cargo/bin:$PATH
script: make
addons:
  apt:
    packages:
    - rpm2cpio
    - genisoimage
    - mono-devel
    - libsystemd-dev
