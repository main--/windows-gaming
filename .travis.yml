language: generic
install:
 - curl -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=stable -y
 - export PATH=$HOME/.cargo/bin:$PATH
script: make
addons:
  apt:
    packages:
    - rpm2cpio
    - genisoimage
    - mono-devel
    - libsystemd-dev
    sources:
    - sourceline: 'deb http://de.archive.ubuntu.com/ubuntu/ xenial main'
